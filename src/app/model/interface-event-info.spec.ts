import { InterfaceEventInfo } from './interface-event-info';

describe('InterfaceEventInfo', () => {
  it('should create an instance', () => {
    expect(new InterfaceEventInfo()).toBeTruthy();
  });
});
