import { PlatformIndependentEvent } from './platform-independent-model';

describe('PlatformIndependentModel', () => {
  it('should create an instance', () => {
    expect(new PlatformIndependentEvent()).toBeTruthy();
  });
});
